package com.citic.server.service.task.tk_etl101;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.context.ApplicationContext;

import com.citic.server.domain.MC00_datasource;
import com.citic.server.domain.MC00_task_fact;
import com.citic.server.utils.FileUtils;

/**
 * 送数过来：
 * 对临时数据目录做初始化
 * 做好数据装载前的预处理
 * @author hubaiqing
 */
public class SendTable extends BaseFile{

	private static final Logger logger = LoggerFactory.getLogger(SendTable.class);
	
	private FileUtils fileUtils = new FileUtils();
	
	public SendTable(ApplicationContext ac,MC00_datasource ds, MC00_task_fact tf) {
		super(ac,ds, tf);
	}

	@Override
	public  boolean run() throws Exception{
		boolean isSuccess = true;

		
		return isSuccess;
    	
    }
}
