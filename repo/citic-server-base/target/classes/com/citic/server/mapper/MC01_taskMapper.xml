<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.citic.server.mapper.MC01_taskMapper">
                
	<select id="getMC01_taskList" resultType="MC01_task">
		select taskid,case when modeltype is null or modeltype='' then '1' else modeltype end as modeltype,taskname,tasksplit,splitparams,taskcmd,taskseq,flag,des
 		from mc01_task where flag='1' order by taskseq asc
	</select>
	
</mapper>


