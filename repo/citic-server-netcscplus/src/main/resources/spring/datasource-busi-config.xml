<?xml version="1.0" encoding="UTF-8"?>
<!-- Repository and Service layers -->
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="  
      http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd         
      http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd             
      http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd         
      http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.2.xsd">

	<bean id="busiDs" class="${citic.datasource.busi.bean-class:com.alibaba.druid.pool.DruidDataSource}" init-method="init" destroy-method="close">
		<property name="driverClassName" value="${citic.datasource.busi.driver-class-name}" />
		<property name="url" value="${citic.datasource.busi.url}" />
		<property name="username" value="${citic.datasource.busi.username}" />
		<property name="password" value="${citic.datasource.busi.password}" />

		<property name="maxActive" value="${citic.datasource.busi.max-active:10}" />
		<property name="initialSize" value="${citic.datasource.busi.initial-size:1}" />
		<property name="maxWait" value="${citic.datasource.busi.max-wait:60000}" />
		<property name="minIdle" value="${citic.datasource.busi.min-idle:1}" />

		<property name="timeBetweenEvictionRunsMillis" value="${citic.datasource.busi.time-between-eviction-runs-millis:60000}" />
		<property name="minEvictableIdleTimeMillis" value="${citic.datasource.busi.min-evictable-idle-time-millis:300000}" />

		<property name="testWhileIdle" value="${citic.datasource.busi.test-while-idle:false}" />
		<property name="testOnBorrow" value="${citic.datasource.busi.test-on-borrow:false}" />
		<property name="testOnReturn" value="${citic.datasource.busi.test-on-return:false}" />
		<property name="validationQuery" value="${citic.datasource.busi.validation-query:select 1 from mp01_dual}" />

		<property name="removeAbandoned" value="${citic.datasource.busi.remove-abandoned:false}" />
		<property name="removeAbandonedTimeout" value="${citic.datasource.busi.remove-abandoned-timeout:1800}" />
		<property name="logAbandoned" value="${citic.datasource.busi.log-abandoned:true}" />

		<property name="poolPreparedStatements" value="${citic.datasource.busi.pool-prepared-statements:true}" />
		<property name="maxOpenPreparedStatements" value="${citic.datasource.busi.max-open-prepared-statements:20}" />

		<property name="useGlobalDataSourceStat" value="${citic.datasource.busi.use-global-datasource-stat:true}" />
		<property name="filters" value="slf4j" />
		<property name="proxyFilters">
			<list>
				<ref bean="stat-filter" />
			</list>
		</property>
	</bean>

	<!-- transaction manage -->
	<bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="busiDs" />
	</bean>

	<!-- 基本jdbc执行器 -->
	<bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
		<property name="dataSource" ref="busiDs" />
	</bean>

	<bean id="vendorProperties" class="org.springframework.beans.factory.config.PropertiesFactoryBean">
		<property name="properties">
			<props>
				<prop key="DB2">db2</prop>
				<prop key="Oracle">oracle</prop>
			</props>
		</property>
	</bean>

	<bean id="databaseIdProvider" class="org.apache.ibatis.mapping.VendorDatabaseIdProvider">
		<property name="properties" ref="vendorProperties"></property>
	</bean>

	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="busiDs" />
		<property name="typeAliasesPackage" value="com.citic.server.domain" />
		<property name="databaseIdProvider" ref="databaseIdProvider" />
		<property name="configLocation" value="classpath:spring/mybatis-busi-config.xml" />
	</bean>

	<bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate"> <!-- scope="prototype" -->
		<constructor-arg index="0" ref="sqlSessionFactory" />
	</bean>

	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="sqlSessionTemplateBeanName" value="sqlSession" />
		<property name="basePackage" value="com.citic.server.*.mapper" />
	</bean>
</beans>